import { Box, styled } from "@mui/material";

export const Wrapper = styled(Box)(
  () => `
    width:100dvw;
    height:100dvh;
    display:flex;
    align-items:center;
    justify-content:center;
    `
);
