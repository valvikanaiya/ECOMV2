import { Box, styled } from "@mui/material";

export const SCEmptyCartWrapper = styled(Box)({
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  height: "100%",
  width: "100%",
});
